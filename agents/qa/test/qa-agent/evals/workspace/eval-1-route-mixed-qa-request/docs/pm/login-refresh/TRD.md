# Login Refresh TRD

Use the existing repository test command `npm test -- login` for unit coverage.
Use the existing acceptance harness only if the environment provides
`QA_BASE_URL`. Do not assume a fixed localhost port.
