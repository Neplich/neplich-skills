# PM Agent Direct Delegation Fixture

This fixture verifies the `pm-agent` entrypoint does not stop at a pure routing
answer for a greenfield PM request. It should route to `idea-to-spec` and
continue directly into the requirement-shaping workflow in the same response.
