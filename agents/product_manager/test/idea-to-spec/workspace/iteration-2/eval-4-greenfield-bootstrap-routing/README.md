# Greenfield Bootstrap Routing Fixture

This workspace is intentionally empty except for eval metadata. The request is
concrete enough to tempt low-discipline models into immediate scaffolding, but
the correct behavior is still PM-first: inspect the empty workspace, keep the
user in `idea-to-spec`, and move toward durable PM docs before any engineering
bootstrap.
